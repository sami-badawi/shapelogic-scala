package org.shapelogic.sc

/**
 * The factory package is a place to combine things
 * Logic should be places other places
 * 
 * E.g. there should not be a dependency on JavaFX spread in most code
 * But it is fine to have dependencies on JavaFX here
 */
package object factory {
  
}